"""
Contains random functions / transformations / etc that were critical
for functionality at some point and took awhile to figure out, but are
no longer immediately relevant to teh code (but I don't want to lose it)
"""

"""Change date from datetime object to string thing"""
##for p in posts:
##    p['date'] = time.strftime("%a, %d %b %Y %H:%M:%S +0000", p['date'])
##    db.Wired.insert(p)
